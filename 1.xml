<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Glassmorphism Hero Section</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: url('https://picsum.photos/1920/1080?blur') no-repeat center center/cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .hero {
      width: 80%;
      max-width: 800px;
      padding: 40px;
      border-radius: 20px;
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      text-align: center;
      color: #fff;
      animation: fadeIn 2s ease;
    }
    .hero h1 {
      font-size: 3rem;
      margin-bottom: 20px;
    }
    .hero p {
      font-size: 1.2rem;
      margin-bottom: 30px;
    }
    .hero button {
      padding: 12px 30px;
      font-size: 1rem;
      border: none;
      border-radius: 30px;
      background: rgba(255, 255, 255, 0.3);
      color: #fff;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .hero button:hover {
      background: rgba(255, 255, 255, 0.6);
      color: #333;
      transform: scale(1.1);
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(50px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <section class="hero">
    <h1>Welcome to GlassWorld</h1>
    <p>Experience the elegance of glassmorphism design</p>
    <button onclick="showMessage()">Explore Now</button>
  </section>

  <script>
    function showMessage() {
      alert("✨ You’ve entered the glass world!");
    }
  </script>
</body>
</html> -->
<!-- 
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Particle Hero Section</title>
  <style>
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: 'Poppins', sans-serif;
      overflow: hidden;
    }
    #particles-js {
      position: absolute;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #1f1c2c, #928dab);
      z-in<!DOCTYPE html>
dex: -1;
    }
    .hero {
      position: relative;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      color: #fff;
      z-index: 1;
    }
    .hero h1 {
      font-size: 3rem;
      margin-bottom: 20px;
      animation: fadeInDown 2s ease;
    }
    .hero p {
      font-size: 1.2rem;
      margin-bottom: 30px;
      animation: fadeInUp 2s ease;
    }
    .hero button {
      padding: 12px 30px;
      border: none;
      border-radius: 30px;
      background: #fff;
      color: #333;
      cursor: pointer;
      transition: transform 0.3s ease;
    }
    .hero button:hover {
      transform: scale(1.1);
      background: #f0f0f0;
    }
    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-50px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(50px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <!-- Particle Background -->
  <!-- <div id="particles-js"></div>

  <!-- Hero Content -->
  <!-- <section class="hero">
    <h1>Welcome to Particle World</h1>
    <p>Interactive hero section with animated particles</p>
    <button onclick="showMessage()">Get Started</button>
  </section> -->

  <!-- Particle.js Script -->
  <!-- <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
  <script>
    particlesJS("particles-js", {
      particles: {
        number: { value: 80 },
        size: { value: 3 },
        move: { speed: 2 },
        line_linked: { enable: true, distance: 150, color: "#ffffff" },
        color: { value: "#ffffff" }
      }
    });

    function showMessage() {
      alert("✨ Particle magic activated!");
    }
  </script>
</body>
</html> --> --> -->
